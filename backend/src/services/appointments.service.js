const Appointments = require("../models/Appointments");

const appointmentsService = {
  getAppointment: async (user) => {},

  addAppointment: async (user) => {},

  deleteApointment: async (user) => {},
};

module.exports = appointmentsService;
